{
  "name": "tmolecule-seo-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv key list --binding=LEARN_PAGES",
    "kv:seed": "bash scripts/seed-kv.sh",
    "kv:seed-preview": "bash scripts/seed-kv.sh --preview"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
