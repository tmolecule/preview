{
  "name": "tmolecule-learn-intel",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that indexes learn.tmolecule.com into Vectorize and powers (1) AI-citation gap monitor and (2) self-internal-linking suggester.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "index": "bash scripts/index-now.sh",
    "link-suggest": "bash scripts/link-suggest.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.90.0"
  }
}
